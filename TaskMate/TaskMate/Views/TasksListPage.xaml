<!-- TasksListPage.xaml -->
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:TaskMate.ViewModels"
             x:Class="TaskMate.Views.TasksListPage"
             x:Name="PageRoot" 
             Title="Ülesannete loend">

    <!-- BindingContext -->
    <ContentPage.BindingContext>
        <vm:TasksListViewModel />
    </ContentPage.BindingContext>

    <StackLayout Padding="10">

        <CollectionView
            ItemsSource="{Binding Tasks}"
            SelectionMode="Single"
            SelectionChanged="OnTaskSelected">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Frame Padding="12" Margin="6" CornerRadius="12" BorderColor="#DDDDDD">
                        <VerticalStackLayout Spacing="4">

                            <Label Text="{Binding Name}"
                                FontSize="16"
                                FontAttributes="Bold" />

                            <Label Text="{Binding DueDate, StringFormat='Tähtaeg: {0:dd.MM.yyyy}'}"
                                FontSize="12"
                                TextColor="Gray" />

                            <Label Text="{Binding Priority}"
                                FontSize="12"
                                TextColor="{Binding PriorityColor}" />

                            <Label Text="Tehtud"
                                IsVisible="{Binding IsDone}"
                                FontSize="12"
                                TextColor="Green" />

                            <HorizontalStackLayout Spacing="8" Margin="0,6,0,0">
                                <Button Text="Edit"
                                        Command="{Binding BindingContext.EditCommand, Source={x:Reference PageRoot}}"
                                        CommandParameter="{Binding .}" />

                                <Button Text="Del"
                                        TextColor="Red"
                                        Command="{Binding BindingContext.DeleteCommand, Source={x:Reference PageRoot}}"
                                        CommandParameter="{Binding .}" />

                                <Button Text="Tehtud"
                                        IsVisible="{Binding IsDone, Converter={StaticResource InverseBoolConverter}}"
                                        Command="{Binding BindingContext.MarkDoneCommand, Source={x:Reference PageRoot}}"
                                        CommandParameter="{Binding .}" />
                            </HorizontalStackLayout>

                        </VerticalStackLayout>
                    </Frame>

                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

    </StackLayout>
</ContentPage>
